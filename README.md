# About

Current project is simple golang backend implementaion with only net/http

# How to test
1. Run `docker compose up`
2. Go to the `http://localhost:8080/swagger/index.html` and play with swagger
